struct Point {
    x, y: int
}

struct Poly {
    pt: Point,

    fn clone(self) -> Self {
        Poly{pt=Point{x=1, y=2}}
    }
}

struct Structure {
    poly: Poly,
    polys: []Poly,
}

fn testFn(pt: Point, poly: Poly, structure: Structure) {}

fn main() {
    var pt = Point{x=2, y=6}
    var poly = Poly{pt}
    var structure = Structure{poly, polys=[poly]}

    var ref = if true do pt else return
    var ref2 = if true do poly.clone() else return
}


