{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build nogc",
            "command": "zig",
            "args": [
                "build",
                "-Dprint-stack",
                "-Dprint-instr",
            ]
        },
        {
            "label": "build gc",
            "command": "zig",
            "args": [
                "build",
                "-Dstress-gc",
                "-Dlog-gc",
                "-Dprint-stack",
                "-Dprint-instr",
            ]
        }
    ]
}