fn getValue() -> ?float { return 5. }
fn getNull() -> ?int { return null }

fn get2(value: int) -> ?int {
    return if value < 5 do value else null
}

fn main() {
    if let v = getValue() {
        print v
    }

    if let n = getNull() {
        print "here"
    }

    var i = 0
    while let v = get2(i) {
        print v
        i += 1
    }
}
