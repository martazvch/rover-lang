struct Point {
    x, y: int
}

struct Poly {
    pt: Point,

    fn clone(self) -> Self {
        return Poly{ pt=Point{ x=1, y=2 } }
    }
}

struct Structure {
    poly: Poly,
    polys: []Poly,
}

fn main() {
    var pt = Point{x=2, y=6}
    var poly = Poly{pt}
    var structure = Structure{poly, polys=[poly]}

    var ref = pt
    var ref1 = structure.poly
    var ref2 = structure.poly.pt
    var ref3 = structure.polys
    var ref4 = structure.polys[0]
    var ref5 = structure.polys[0].pt

    var pts = [pt]
    var structs = [structure.poly.pt]

    ref2 = (structure.poly.pt)
    ref2 = { break { break structure.poly.pt } }
    ref2 = { break ( { break ( structure.poly.pt ) } ) }

    ref2 = if true do return else poly.pt
    ref2 = if true do poly.pt else return
}

