struct Foo {
    x, y = 0

    fn init() -> Self {
        return Self{} 
    }
}

struct Bar {
    a: []bool

    fn init() -> Self {
        return Self {a=[true]}
    }
}

fn main() {
    struct Baz {
        flag = true
        
        fn init() -> Self {
            return Self {flag=false}
        }
    }
}
